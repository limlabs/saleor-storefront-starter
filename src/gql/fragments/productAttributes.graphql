fragment ProductDetailsAttributeFragment on SelectedAttribute {
  attribute {
    id
    name
    translation(languageCode: $languageCode) {
      name
    }
  }
  values {
    id
    name
    translation(languageCode: $languageCode) {
      name
    }
  }
}
